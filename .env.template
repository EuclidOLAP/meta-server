# Environment variables for meta-server
# Required for DB connection
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=euclid_meta
DB_DIALECT=mysql
# Optional DB port
DB_PORT=3306

# JWT secrets (override defaults in src/config/jwt.ts if set)
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d

# Feature toggles used in index.ts
UPLOAD_MEASURES_TO_ALIYUNOSS_ENABLED=false
GEN_VCE_MEASURES_ENABLED=false

# OSS / upload batch variables (used by batch scripts)
OSSTBM_CUBE_GID=0
OSSTBM_BEGINNING_POSITION=0
OSSTBM_EXPECTED_MEASURE_RECORDS=1000
OSSTBM_BUCKET_REGION=
OSSTBM_BUCKET_NAME=
OSSTBM_ACCESS_KEY_ID=
OSSTBM_ACCESS_KEY_SECRET=

# Node environment
NODE_ENV=development
